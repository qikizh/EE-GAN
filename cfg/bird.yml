CONFIG_NAME: 'bird'

DATASET_NAME: 'birds'
DATA_DIR: '../data/birds/'
SAVE_DIR: '../data/SSA_GAN_OUT'
GPU_ID: 0
WORKERS: 4

B_VALIDATION: True # True  # False
RESTORE: False
loss: 'hinge'

TREE:
    BRANCH_NUM: 1
    BASE_SIZE: 256

TRAIN:
    NF: 48 # default 64
    BATCH_SIZE: 32  #24
    MAX_EPOCH: 700
    NET_G: '../data/SSA_GAN_OUT/12_20/train_finetune_ssa/Model/netG_400.pth'
    # '../data/Models/SSA_GAN/cub/netG_590.pth'
    # '../data/SSA_GAN_OUT/12_2/base/Model/netG_580.pth'
    # # when validation, put the path of the trained model here
    WARMUP_EPOCHS: 400
    GSAVE_INTERVAL: 20
    DSAVE_INTERVAL: 50


TEXT:
    EMBEDDING_DIM: 256
    CAPTIONS_PER_IMAGE: 10
    DAMSM_NAME: '../data/SSA_GAN_OUT/12_20/train_finetune_ssa/Model/text_enc_400.pth'
    # '../data/Models/AttnGAN/text_encoder200.pth'

GAN:
    GF_DIM: 48
    DF_DIM: 48